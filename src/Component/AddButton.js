const AddButton = ({ changeDisplay }) => {
	return (
		<button onClick={changeDisplay} className="material-symbols-outlined coloured-button-style button-bottom-float ">
			add
		</button>
	);
};

export default AddButton;
